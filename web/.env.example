# Copy this file to .env.local and fill in real values. .env.local is gitignored.

# 21st.dev registry API key — required for `npx shadcn@latest add @21st/<author>/<slug>`.
# Get one at https://21st.dev/mcp
API_KEY_21ST=
